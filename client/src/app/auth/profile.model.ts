export class UserProfile {
  constructor(public email: string,
              public age: number,
              public name:string,
              public country:string,
              public contact:string,
              ) {
  }
}